import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Página única de candidatura — sem imagens externas, sem CMS, sem gateway
  // de pagamento. Mantido deliberadamente mínimo e isolado do site principal.
};

export default nextConfig;
